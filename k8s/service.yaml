apiVersion: v1
kind: Service
metadata:
  name: vessel-operations-service
  namespace: freight-forwarder
  labels:
    app: vessel-operations-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: vessel-operations-service

# Made with Bob
